/* ─── src/features/skills/types.ts ─────────────────────────── */

export interface Skill {
  id: string;
  name: string;
  created_at: string;
}

export interface JobCategory {
  id: string;
  name: string;
  created_at: string;
}